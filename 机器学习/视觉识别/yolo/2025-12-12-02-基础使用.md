# 训练

## Yolo基础

有四种模式

+ Train: 训练模式
+ Validation: 验证模式
+ Predict: 预测模式
+ Export: 导出

任务

+ Detect检测

![image-20251212101557400](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202512121015641.png)

物体位置识别

+ Segmentation分割

![image-20251212101643301](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202512121016542.png)

边缘识别

+ Classification 分类
+ Pose: 姿态检测
+ OBB

![image-20251212101838502](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202512121018698.png)

## 基础使用

### 检测

[Object Detection - Ultralytics YOLO Docs](https://docs.ultralytics.com/tasks/detect/)

Yolo支持的基础模型如下

![image-20251212102449016](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202512121024134.png)

可以使用这里的模型进行基本的预测

```python
from ultralytics import YOLO

# Load a model
model = YOLO("yolo11n.pt")  # load an official model
model = YOLO("path/to/best.pt")  # load a custom model

# Predict with the model
results = model("https://ultralytics.com/images/bus.jpg")  # predict on an image

# Access the results
for result in results:
    xywh = result.boxes.xywh  # center-x, center-y, width, height
    xywhn = result.boxes.xywhn  # normalized
    xyxy = result.boxes.xyxy  # top-left-x, top-left-y, bottom-right-x, bottom-right-y
    xyxyn = result.boxes.xyxyn  # normalized
    names = [result.names[cls.item()] for cls in result.boxes.cls.int()]  # class name of each box
    confs = result.boxes.conf  # confidence score of each box
```

也可以使用命令行

```bash
yolo detect predict model=yolo11n.pt source='https://ultralytics.com/images/bus.jpg' # predict with official model
yolo detect predict model=path/to/best.pt source='https://ultralytics.com/images/bus.jpg' # predict with custom model
```

### 训练

在实际训练的时候, 需要指定出来不同的层的大小以及作用, 所以需要一个配置文件, 由于模块封装的比较好, 添加模块不需要理解其他部分的代码, 读取配置文件即可知道怎么运行
