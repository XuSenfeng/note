# SO101

## 查找端口

```bash
lerobot-find-port
```



## 控制

### 跟随

```bash
lerobot-teleoperate \
    --robot.type=so101_follower \
    --robot.port=/dev/tty.usbmodem5AA90243381 \
    --robot.id=my_awesome_follower_arm \
    --teleop.type=so101_leader \
    --teleop.port=/dev/tty.usbmodem5AA90242591 \
    --teleop.id=my_awesome_leader_arm
```

### 记录消息

```bash
lerobot-record \
  --robot.type=so101_follower \
  --robot.port=/dev/tty.usbmodem5AA90243381 \
  --robot.id=my_awesome_follower_arm \
  --teleop.type=so101_leader \
  --teleop.port=/dev/tty.usbmodem5AA90242591 \
  --teleop.id=my_awesome_leader_arm \
  --dataset.repo_id=./so101_try_001 \
  --dataset.single_task="pick and place test" \
  --dataset.num_episodes=1 \
  --dataset.episode_time_s=20 \
  --dataset.reset_time_s=5 \
  --dataset.push_to_hub=false \
  --display_data=true
```

```bash
(lerobot) jiao@jiaodeMacBook-Air lerobot % lerobot-replay \
  --robot.type=so101_follower \
  --robot.port=/dev/tty.usbmodem5AA90243381 \
  --robot.id=my_awesome_follower_arm \
  --dataset.repo_id=./so101_try_001 \
  --dataset.episode=0
```

文件记录在`/Users/jiao/.cache/huggingface/lerobot/so101_try_00*`



