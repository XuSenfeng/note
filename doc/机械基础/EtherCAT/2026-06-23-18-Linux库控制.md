# LInux库控制

实际使用的库是 `libethercat`

IgH EtherCAT为运行于Linux系统的开源`EtherCAT`主站程序，`IgH EtherCAT`主站通过构建Linux字符设备，应用程序通过对字符设备的访问实现与EtherCAT主站模块的通信。

IgH EtherCAT开发包配套EtherCAT工具，该工具提供各种可在Linux用户层运行的命令，可直接实现对从站的访问和设置，如设置从站地址、显示总线配置、显示PDO数据、读写SDO参数等。

https://docs.etherlab.org/ethercat/1.5/doxygen/index.html 文档
https://gitlab.com/etherlab.org/ethercat/-/tree/stable-1.6 源码

## 运行基础

### 库文件

实际运行的时候需要使用的文件

+ 驱动部分: phylink.ko物理层链路层的通信, pcs-xpcs.ko支持网卡功能, ec_master.ko内核模块用于支持EtherCAT通信, ec_stmmac.ko底层实时性优化网口驱动
+ 用户部分: ethercat, libethercat.so, 一个调试工具以及一个用户层调用接口
+ 上层应用

### 内核

EtherCAT IgH需要保证高实时性，Preempt-RTRT是一种针对实时性能进行了优化的Linux内核

1. 实时性能： Preempt-RT提供了更可靠和更精确的实时性能。它采用了一些实时调度策略和机制，使得任务能够按照严格的时间要求执行，从而适用于需要高度可预测性和低延迟的应用场景，如工业自动化、机器人控制等。
2. 硬实时能力： Preempt-RT具有硬实时能力，即能够确保任务在规定的时间内完成，而不会受到其他任务或中断的干扰。这对于需要严格的时间限制的应用非常重要，如航空航天、医疗设备等领域。
3. 任务调度： Preempt-RT使用了更加高效和优化的任务调度算法，如基于优先级的实时调度算法，以确保高优先级任务能够及时响应并完成，而低优先级任务不会影响到实时任务的执行。
4. 中断处理： Preempt-RT针对中断处理进行了优化，使得中断的响应时间更短，能够更快地响应外部事件。
5. 内核定时器： Preempt-RT提供了更精确和可配置的内核定时器，使得可以实现微秒级的定时精度，适用于对时间要求极高的应用场景。
6. 实时扩展： Preempt-RT提供了一些实时扩展机制，使得用户能够方便地对内核进行定制和扩展，以满足特定应用的需求。

## 移植

1. 打实时性补丁, 可以参考文档SDK/docs/Patches/Real-Time-Performance/README.md
2. 开启内核配置, 把对应的模块编译出来
3. 加载模块
4. 使用soc的性能模式

## 测试应用

可以使用命令`ethercat`进行测试

```bash
# 设置别名
ethercat alias -p 0 0

# 设置别名地址, 把别名0改为1
ethercat alias -a 0 1

# 输出PDO的信息, C语言格式
ethercat cstruct

# 查看从站信息
ethercat slaves -v

# 查看PDO信息
sr@SRC1100:~$ sudo ethercat pdos
=== Master 0, Slave 0 ===
SM0: PhysAddr 0x1000, DefaultSize  128, ControlRegister 0x26, Enable 1
SM1: PhysAddr 0x1080, DefaultSize  128, ControlRegister 0x22, Enable 1
SM2: PhysAddr 0x1100, DefaultSize    9, ControlRegister 0x64, Enable 1
  RxPDO 0x1600 "OUT_GENERIC process data mapping"
    PDO entry 0x6040:00, 16 bit, "Controlword"
    PDO entry 0x6071:00, 16 bit, "Target_torque"
    PDO entry 0x60ff:00, 32 bit, "Target_velocity"
    PDO entry 0x607a:00, 32 bit, "Target_position"
    PDO entry 0x6081:00, 32 bit, "Profile_velocity"
    PDO entry 0x6060:00,  8 bit, "Modes_of_operation"
    PDO entry 0x2302:00, 16 bit, "CurModSpd_limitScal"
SM3: PhysAddr 0x1400, DefaultSize    9, ControlRegister 0x20, Enable 1
  TxPDO 0x1a00 "IN_GENERIC process data mapping"
    PDO entry 0x6041:00, 16 bit, "Statusword"
    PDO entry 0x603f:00, 16 bit, "Error_Code"
    PDO entry 0x6078:00, 16 bit, "Current_actual_value"
    PDO entry 0x606c:00, 32 bit, "Velocity_actual_value"
    PDO entry 0x6064:00, 32 bit, "Position_actual_angle_value"
    PDO entry 0x6061:00,  8 bit, "Modes_of_operation_display"
    PDO entry 0x6000:00,  8 bit, "SubIndex 000"
    PDO entry 0x2300:00, 16 bit, "Bridge_Temp"
    PDO entry 0x2301:00, 16 bit, "Motor_Temp"
    PDO entry 0x60fd:00, 32 bit, "Digital_inupts"
=== Master 0, Slave 1 ===
SM0: PhysAddr 0x1000, DefaultSize  256, ControlRegister 0x26, Enable 1
SM1: PhysAddr 0x1400, DefaultSize  256, ControlRegister 0x22, Enable 1
SM2: PhysAddr 0x1800, DefaultSize   12, ControlRegister 0x64, Enable 1
  RxPDO 0x1600 ""
    PDO entry 0x6040:00, 16 bit, ""
    PDO entry 0x607a:00, 32 bit, ""
    PDO entry 0x60ff:00, 32 bit, ""
    PDO entry 0x6081:00, 32 bit, ""
    PDO entry 0x6060:00,  8 bit, ""
    PDO entry 0x6098:00,  8 bit, ""
SM3: PhysAddr 0x1c00, DefaultSize   28, ControlRegister 0x20, Enable 1
  TxPDO 0x1a00 ""
    PDO entry 0x603f:00, 16 bit, ""
    PDO entry 0x6041:00, 16 bit, ""
    PDO entry 0x606c:00, 32 bit, ""
    PDO entry 0x6064:00, 32 bit, ""
    PDO entry 0x6061:00,  8 bit, ""
    PDO entry 0x6061:00,  8 bit, ""
```
**SM（Sync Manager，同步管理器）**：EtherCAT 从站的硬件数据通道，共 4 个标准通道，分工明确：
- SM0/SM1：**邮箱通道**，传输非周期数据（比如配置参数、SDO 读写、故障告警），不参与周期实时控制。
- SM2/SM3：**过程数据通道**，传输周期实时数据，也就是 PDO 数据，是运动控制的核心。
- **RxPDO**：站在从站视角，是「从站接收的数据」，也就是**主站→从站的控制指令**（比如目标位置、控制字）。
- **TxPDO**：站在从站视角，是「从站发送的数据」，也就是**从站→主站的状态反馈**（比如实际位置、状态字）。
**对象字典索引**：比如 `0x6040:00`，是 CoE 协议的标准寻址方式，冒号前是主索引，冒号后是子索引。`0x6000~0x6FFF` 是 CiA 402 伺服标准定义的对象，`0x2000~0x2FFF` 是厂商自定义对象。
**PDO 映射**(`0x1600`/`0x1a00`)规定:"把哪些对象、按什么顺序,打包进这个从站的过程数据区"
## API控制流程

1. 使用命令启动服务, 开启ethernet设备
2. 初始化申请一个`master`
3. 创建`domain`的实际数据发送使用的内存空间
4. 创建从站的数据句柄
5. 注册 PDO 条目, 协调实际使用的是哪些数据
6. 激活主站, 主站把总线上**所有从站**的过程数据拼进一整块连续内存 `domain_pd`。每个从站的数据块在这块大内存里有个基址,加上对象在从站内的位置,就得到**这个对象在 domain 里的最终字节偏移**
7. 获取实际的映射指针
8. -循环-
9. 接受数据
10. 处理数据
11. 写入控制字
12. 输出排入队列
13. 发送
14. -结束循环-
## API函数


## 示例

```c
#include <ecrt.h>
#include <unistd.h>

/* ───── 1. 全局句柄 ───── */
static ec_master_t  *master    = NULL;   // 主站
static ec_domain_t  *domain    = NULL;   // 过程数据域
static uint8_t      *domain_pd = NULL;   // 域的进程映像指针

/* 从站身份 (按你的设备改) */
#define ALIAS       0
#define POSITION    0                 // 总线位置
#define VENDOR_ID   0x00000168
#define PRODUCT_ID  0x00000011

/* ───── 2. 要交换的 PDO 条目, 偏移会被自动填进这两个变量 ───── */
static unsigned int off_ctrl;         // 0x6040 控制字 (输出)
static unsigned int off_status;       // 0x6041 状态字 (输入)

/* ───── 3. 注册表: "把这些对象登记到 domain, 偏移写回 offset 变量" ───── */
const static ec_pdo_entry_reg_t domain_regs[] = {
    {ALIAS, POSITION, VENDOR_ID, PRODUCT_ID, 0x6040, 0x00, &off_ctrl},
    {ALIAS, POSITION, VENDOR_ID, PRODUCT_ID, 0x6041, 0x00, &off_status},
    {}                                // 结束标记(全0)
};

int main(void)
{
    /* ① 请求主站 (索引0) */
    master = ecrt_request_master(0);
    if (!master) return -1;

    /* ② 创建过程数据域 */
    domain = ecrt_master_create_domain(master);
    if (!domain) return -1;

    /* ③ 获取从站配置句柄 */
    ec_slave_config_t *sc =
        ecrt_master_slave_config(master, ALIAS, POSITION, VENDOR_ID, PRODUCT_ID);
    if (!sc) return -1;

    /* ④ 注册 PDO 条目 (算偏移)， 实际的偏移记录在给的指针里面 */
    if (ecrt_domain_reg_pdo_entry_list(domain, domain_regs)) return -1;

    /* ⑤ 激活主站 (之后不能再改配置) */
    if (ecrt_master_activate(master)) return -1;

    /* ⑥ 拿到进程映像指针 */
    domain_pd = ecrt_domain_data(domain);
    if (!domain_pd) return -1;

    /* ⑦ 实时循环 */
    while (1) {
        ecrt_master_receive(master);   // 收: 把网线上回来的数据搬进映像
        ecrt_domain_process(domain);   // 处理输入

        /* —— 在这里读写过程数据 —— */
        uint16_t status = EC_READ_U16(domain_pd + off_status);  // 读状态字
        EC_WRITE_U16(domain_pd + off_ctrl, 0x000F);             // 写控制字
        (void)status;

        ecrt_domain_queue(domain);     // 把输出排进队列
        ecrt_master_send(master);      // 发: 把映像发到网线

        usleep(1000);                  // 1ms 周期
    }
    return 0;
}
```