# 一致性测试

ETG 不为主机设备颁发证书

| 路径                            | 依据文档                                  | 结果                           | 认证/证书                                       |
| ----------------------------- | ------------------------------------- | ---------------------------- | ------------------------------------------- |
| **A. 主机"互通性验证"**(推荐、实操)       | **ETG.1500(主机分级)** + **ETG.7010(指南)** | 到 ETC 或 Plug Fest 上**跑互联测试** | **无证书**,但有"符合 Class A/B + Feature Pack"的自声明 |
| **B. 主机自声明"符合 Master Class"** | 核对 **ETG.1500 的 Table 1** 要求清单        | 对照勾选                         | 无官方证书,靠 vendor 承诺                           |
> 主要需要关注的是 ETG1500 EtherCAT Master Classes

你不是"测一致性",而是"**声明你的主机属于哪个等级(Class)**"。ETG.1500 定义了:

主机分两级(第 4 章)

- **Class A:标准 EtherCAT 主机** —— 支持 ETG.1000 系列全部 + ETG.1020 大部分功能,**推荐追求的目标**。
- **Class B:最小 EtherCAT 主机** —— 功能精简,只要求能跑大部分设备(支持 CoE + 周期 PDO 交换)。只在资源受限(如嵌入式)时允许用。

![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260909154153958.png?imageSlim)
ETG.1500 §4.3 对 Class B 的定义就是"运行大多数 EtherCAT 设备所需的主要功能(例如支持 CoE、周期性过程数据交换)",并且明确只在资源受限、无法做到 Class A 时才允许宣称(嵌入式系统正是它举的例子)。电机控制器 + 只做 CoE,就是这一档。

| ID   | 项目                  | Class B         | 测什么                                                                                                                                                                               |
| ---- | ------------------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 101  | Service Commands    | 支持 ENI 导入时必须全支持 | 若只做内部配置,可裁剪命令种类(§5.3.1)                                                                                                                                                           |
| 103  | Device Emulation 从站 | 必须              | 带/不带应用控制器的从站都能带起来;**对 emulation=true 的从站不要置 Error Indication Acknowledge 位**(§5.3.3) \| Support Slaves with and without application  controller                                   |
| 104  | ESM                 | 必须              | INIT→PREOP→SAFEOP→OP 全迁移;超时值取自 ESI/SII,缺失则用 ETG.1020 默认值;OpOnly 标志置位时非 OP 态要关掉所有输出 SM \| Support of ESM special behaviour                                                         |
| 105  | 错误处理                | 必须              | WKC 校验、丢帧、从站掉线;必须给应用层提供访问错误/诊断信息的接口(§5.3.5)  Checking of network or slave errors, e.g. \| Working  <br>Counter                                                                    |
| 107  | EtherCAT 帧类型        | 必须              | EtherType 0x88A4、EtherCAT Header Type=1 \| Support EtherCAT Frames                                                                                                                |
| 201  | 周期 PDO              | 必须              | 周期性过程映像刷新 \| Cyclic process data exchange                                                                                                                                         |
| 301  | 在线扫描 **或** 读 ENI    | 二者至少一个          | Network configuration functionality included in  EtherCAT Master \| Network Configuration taken from ENI file                                                                     |
| 302  | 启动时比对网络配置           | 必须              | VendorID / ProductCode / RevisionNo / SerialNo、Identification、拓扑 \| Compare configured and existing network  configuration during boot-up                                         |
| 305  | EEPROM 访问           | 读必须(写可选)        | 通过 ESC 寄存器访问 SII,含访问状态机 \| Support routines to access EEPROM via ESC  register                                                                                                    |
| 401  | 邮箱                  | 必须              | 基本邮箱传输 \| Main functionality for mailbox transfer                                                                                                                                 |
| 402  | 邮箱可靠层 RMSM          | 必须              | 丢帧重发恢复 \| Support underlying resilient layer                                                                                                                                      |
| 404  | 邮箱轮询                | 必须              | 按 ENI 的 PollTime 轮询输入邮箱,或用 FMMU 映射 SM 状态位 0x0805.0 \| Polling Mailbox state in slaves                                                                                             |
| 501  | SDO 上/下载            | 必须              | expedited + normal 两种都要 \| Normal and expedited transfer                                                                                                                          |
| 505  | Emergency           | 必须              | 接收 EMCY——**对电机控制器这条最关键,驱动器故障就靠它上报** \| Receive Emergency messages                                                                                                                 |
| 1101 | DC支持                | 支持DC的时候必须       | 支持分布时钟 \| Support of Distributed Clock                                                                                                                                            |
| 1201 | 从站到从站通信             | 必须              | 主站栈或应用做数据搬运 \| Information is given in ENI file or can be part of any  other network configuration  <br>Copying of the data can be handled by master stackor master’s application |
Class B 里降为"宜"(should)的,建议你还是做:

- **502 分段传输**:读长对象、字符串、SDO Info 响应超过邮箱长度时必然用到
- **503 Complete Access**:注意——**支持 ENI 导入的话这条是 shall**。你自己那个 JUXIE 的 ESI 里 `CompleteAccess="1"`,并且 PDO 映射对象整体写入通常就走 CA
- **504 SDO Info**:在线扫描模式下要读从站对象字典,基本躲不开
- 102 IRQ 位、303 显式设备识别(热插拔/防插错线)、1102/1103 DC 传输延迟与同步窗口监视

## 测试段的采购清单(+§4.1)  ETG7000_3_V1i0i0_D_R_InteroperabilityTestDirective.pdf

文档对从站段有硬门槛,两条前提:

- 每个从站**必须已在 ETC 通过一致性测试**——你自己的 JUXIE 不满足这条,不能算入测试段
- 每个从站必须在默认模式下、**无需手动加启动命令**就能进 OP

要求覆盖的维度:

- **拓扑**:星型(4 端口设备)、线型(2 端口 MII)、菊花链(3 端口全接)、两从站间 100m 网线
- **ESC 型号**:ET1100、ET1200、Altera IP Core、ESC10/ESC20、Xilinx IP Core、netX100、netX50、TI
- **DC**:仅 32 位、64 位、无接收时间寄存器三种
- **标识**:直接 ID 机制、请求 ID 机制、无 ID 选择器
- **过程数据**:纯输入、纯输出、输入输出兼有,以及支持 Modules/Slots 的 ESI
- **邮箱协议**:CoE(§4.1.5 只列了 CoE,对你正好)

4.2 还列了一批专门用来踩坑的设备,其中几个对 CoE-only 主站特别值得凑:

- 默认使能 emergency 对象的设备 / 完全不支持 emergency 的设备
- 8/16/32 位 ENUM SDO,**其中至少一个要有 255 个 ENUM 字符串**
- "慢速"设备:邮箱响应 ≥10 秒,整个对象字典列表传输 ≥30 秒 —— 专门打超时和分段传输
- 修订号字大小端相反的两种设备 —— 打配置比对(302)
- 完全中断驱动 PDI 的设备(INIT/PREOP 下不访问 ESC,PDI 看门狗会超时)vs 轮询 PDI 的设备
- 复杂设备但无过程数据 / 过程数据可变(ESI 里没有 PDO 尺寸信息)/ 仅支持 PDO upload(ESI 里没有 PDO 条目和字典,靠 `Mailbox:CoE@PdoUpload=1` + SDO Info)
- **拒绝 SAFEOP→OP 除非输出 SM 在看门狗超时内被写过至少一次**的从站

最后一条几乎是必配 —— 很多自研主站就是在这里卡住,因为它们在切 OP 之前不发过程数据。