---
tags:
  - 导航
---
# Dijkstra最短路径

Dijkstra算法（迪杰斯特拉算法）用于在给定图中查找某个顶点到其它顶点的最短路径
在给定的一张图中，一个顶点到另一个顶点的路径可能有多条，最短路径指的就是顶点之间“最短”的路径。

![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260604141210098.png?imageSlim)
上图不仅是一张图，顶点之间的边还带有权值，比如 V1 到 V2 这条边的权值为 5，因此更确切的说，这是一张网，而且是有向网

- V0 -> V5，总权值为 100
- V0 -> V4 -> V5，总权值为 30+60 = 90
- V0 -> V4 -> V3 -> V5，总权值为 30+20+10 = 60
- V0 -> V2 -> V3 -> V5，总权值为 10+50+10 = 70

> 用Dijkstra算法查找最短路径时，必须保证图中所有边（弧）的权值为非负数，否则会导致查找失败

## 实际实现

1. 从V0开始, 查看到达其他不同节点的距离, 不存在的标记为无穷大

![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260604141518328.png?imageSlim)

> 表格中 V0->V2 的权值最小，可以断定 V0 到 V2 的最短路径就是 V0->V2

2. 找到 V0->V2 最短路径后，继续从 V2 出发，统计从 V0 经过 V2 再到达其它顶点的路径权值，如果比之前记录的权值更小，表明找到了一条更短的路径，将此路径信息更新到表格中

![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260604141640682.png?imageSlim)

> 找到了一条从 V0 到 V3 更短的路径, 除了已知的最短路径外，表 3 中 V0->V4 的权值最小，所以 V0 到 V4 的最短路径就是 V0->V4
> ![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260604141733850.png?imageSlim)
3. 找到 V0->V4 最短路径后，继续从 V4 出发，统计从 V0 经过 V4 再到达其它顶点的路径权值，如果比表 4 记录的权值更小，表明找到了一条更短的路径，将此路径信息更新到表格
![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260604141751273.png?imageSlim)
> 查找到两个更短的路径, 进行更新, 同时记录现在的最短路径
> ![image.png](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-xiangong/20260604141857742.png?imageSlim)

## 代码实现

```c

#include <stdio.h>
#define MAX_VERtEX_NUM 20                   //顶点的最大个数（限制图的规模）
#define VRType int                          //表示弧的权值的类型
#define VertexType int                      //图中顶点的数据类型
#define INFINITY 65535
#define AUTHOR xiexuewu.github.io

typedef struct {
	// 一个下标翻译, 把代码里面的数组下标翻译为另一个数字/字母/字符串
    VertexType vexs[MAX_VERtEX_NUM];               //存储图中顶点数据
    VRType arcs[MAX_VERtEX_NUM][MAX_VERtEX_NUM];   //二维数组，记录顶点之间的关系
    int vexnum, arcnum;                            //记录图的顶点数和弧（边）数
}MGraph;

typedef int PathMatrix[MAX_VERtEX_NUM];      //此类型数组用来存储最短路径中经过的顶点的下标
typedef int ShortPathTable[MAX_VERtEX_NUM];  //此类型数组用来存储各个最短路径的权值和

// 根据顶点的值，找到它在顶点数组中的下标（邻接矩阵的索引）字典翻译
int LocateVex(MGraph* G, VertexType v) {
    int i = 0;
    //遍历一维数组，找到变量v
    for (; i < G->vexnum; i++) {
        if (G->vexs[i] == v) {
            break;
        }
    }
    //如果找不到，输出提示语句，返回-1
    if (i > G->vexnum) {
        printf("no such vertex.\n");
        return -1;
    }
    return i;
}

//从标准输入里面获取数据构造有向网
void CreateUDG(MGraph* G) {
    int i, j, v1, v2, w, n, m;
    // 获取这个图的大小
    scanf("%d,%d", &(G->vexnum), &(G->arcnum));
    for (i = 0; i < G->vexnum; i++) {
        scanf("%d", &(G->vexs[i])); // 输入翻译字典
    }
    for (i = 0; i < G->vexnum; i++) {
        for (j = 0; j < G->vexnum; j++) {
            G->arcs[i][j] = INFINITY;
        }
    }
    // 实际的权重的配置输入
    for (i = 0; i < G->arcnum; i++) {
        scanf("%d,%d,%d", &v1, &v2, &w);
        n = LocateVex(G, v1);
        m = LocateVex(G, v2);
        if (m == -1 || n == -1) {
            printf("no this vertex\n");
            return;
        }
        G->arcs[n][m] = w;
    }
}

//迪杰斯特拉算法，v0表示有向网中起始点所在数组中的下标
void ShortestPath_Dijkstra(MGraph G, int v0, PathMatrix P, ShortPathTable D) {
    int final[MAX_VERtEX_NUM] = { 0 };//用于存储各顶点是否已经确定最短路径的数组
    int k = 0, v, i, w;
    //对各数组进行初始化
    for (v = 0; v < G.vexnum; v++) {
        D[v] = G.arcs[v0][v];
    }
    //由于下标v0的顶点为起始点，所以不用再判断
    D[v0] = 0;
    final[v0] = 1;
    for (i = 0; i < G.vexnum; i++) {
        int min = INFINITY;
        // 找「未确定」的距离最小的顶点 k
        for (w = 0; w < G.vexnum; w++) {
            if (!final[w]) {
                if (D[w] < min) {
                    k = w; // 使用k记录当前的最短路径
                    min = D[w];
                }
            }
        }
        //设置该顶点的标志位为1，避免下次重复判断
        final[k] = 1;
        //对v0到各顶点的权值进行更新
        for (w = 0; w < G.vexnum; w++) {
            if (!final[w] && (min + G.arcs[k][w] < D[w])) {
                D[w] = min + G.arcs[k][w];
                P[w] = k;//如果<k,w>是组成最短路径的部分，只需要将 k 存储在 P 数组相应位置即可
            }
        }
    }
}

//递归输出顶点之间最短路径的具体线路
void printPath(PathMatrix P, int i)
{
    int k = P[i];
    if (k == 0)
        return;
    printPath(P,k);
    printf("V%d-", k);
}

// 输出 v0 和各个顶点之间的最短路径
void printMatrix(MGraph G, int v0, PathMatrix P, ShortPathTable D) {
    int i, j;
    for (i = 0; i < G.vexnum; i++) {
        if (i == v0) {
            continue;
        }
        printf("V%d-V%d: 最短路径为:", v0, i);
        if (D[i]== INFINITY)
            printf("%s\n", "INF");
        else {
            printf("%d", D[i]);
            printf("，依次经过：V%d-", v0);
            //调用递归函数
            printPath(P, i);
            printf("V%d\n", i);
        }
    }
}

int main() {
    PathMatrix P = { 0 };
    ShortPathTable D = { 0 };
    MGraph G;
    CreateUDG(&G);
    ShortestPath_Dijkstra(G, 0, P, D);
    printMatrix(G, 0, P, D);
    return 0;
}
```