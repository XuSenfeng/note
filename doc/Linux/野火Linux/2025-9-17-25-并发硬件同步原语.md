## 硬件同步原语

## 关中断

很少使用

```c
local_irq_disable()
local_irq_enable()
```

## 内存屏障

用于控制内存访问顺序的技术。它主要用于多处理器系统中，确保某些操作按预期顺序执行，避免 CPU 和编译器对内存访问进行优化，从而影响程序的正确性。它的作用是**禁止 CPU 重新排序特定的内存操作**。它确保在屏障之前的所有读/写操作在屏障之后的操作之前完成。

1. **全屏障（Full Barrier 或者 LFENCE、SFENCE）**：

- **作用**：以下两种相加。
- **用途**：确保所有的内存操作都在内存屏障前完成，通常用于同步和锁定操作。
- **内核函数**：`mb() `(Memory Barrier)

**2.读屏障（Read Barrier 或者 LFENCE）**：

- **作用**：保证屏障之前的所有读操作在屏障之后的读操作之前完成。---》“完成”是缓存同步主内存的意思。
- **本质**：作用是强制将 CPU**核心** 中的 **L1/L2 缓存** 中的共享变量值写回到 **主内存。**
- **用途**：在执行并行读操作时确保读顺序。
- **内核函数**：`rmb()` (Read Memory Barrier)

**3.写屏障（Write Barrier 或者 SFENCE）**：

- **作用**：保证屏障之前的所有写操作在屏障之后的写操作之前完成。--》“完成”是主内存同步到缓存的意思。
- **本质**：作用是强制使数据从主内存加载，而不是直接使用可能已经过时的缓存数据。
- **用途**：用于确保写操作顺序。
- **内核函数**：`wmb()` (Write Memory Barrier)

**4.无序屏障（No-op Barrier）**：

- **作用**：没有实际影响，仅确保 CPU 不会重排序特定的指令。
- **用途**：常用于确保指令的顺序性而不做其他强制性的内存同步。

## 原子操作

### 原子整型操作接口

![image-20251109230746286](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109230746286.png)

原子操作的各种实现实际是更具不同的架构是不同的

#### 原子整型变量

```c
typedef struct {
int counter;
} atomic_t;
```

##### ATOMIC_INIT()

定义整型原子变量并设置初始值

```c
#define ATOMIC_INIT(i) { (i) }
atomic_t data = ATOMIC_INIT(int i)
```

WRITE_ONCE 宏保证这个写操作不会被编译器优化或重排序

##### atomic_set()

用于设置整型原子变量的值

```c
atomic_set(atomic_t *v,int i)
```

这个操作并不是原子操作, 只是使用volatile保证确实是操作到这个数据

##### atomic_read()

获取原子变量的值

```c
#define atomic_add(i,v)			(void)atomic_add_return((i), (v))
atomic_read(atomic_t *v)
```

##### atomic_add()/atomic_sub()

整型原子变量的加/减

```c
static inline void atomic_add(int i, atomic_t *v) //整型原子变量加i
static inline void atomic_sub(int i, atomic_t *v) //整型原子变量减i
```

##### atomic_inc()/atomic_dec()

整型原子变量自增/自减

```c
static inline void atomic_inc(atomic_t *v); //整型原子变量自增1
static inline void atomic_dec(atomic_t *v); //整型原子变量自减1
// 返回成功以后得数值
static __always_inline int atomic_inc_return(atomic_t *v);
static __always_inline int atomic_dec_return(atomic_t *v);
// 判断是不是 == 0
static __always_inline bool atomic_inc_and_test(atomic_t *v);
static __always_inline bool atomic_dec_and_test(atomic_t *v);
```

#### atomic_xchg()

它会把你传入的新值（如 0 或 1）写入 atomic_t 变量，并返回原来的旧值。整个过程是原子的，保证不会被中断或并发修改

```c
static __always_inline int atomic_xchg(atomic_t *v, int i);
```

#### 位原子操作函数

![image-20251109230728218](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109230728218.png)

##### set_bit()

将某一位置1

```c
set_bit(int nr, unsigned long *addr)
```

##### clear_bit()

将某一位置0

```c
clear_bit(int nr, unsigned long *addr)
```

##### change_bit()

反转某一位的值

```c
int test_bit(int nr, unsigned long \*addr)
```

### 实际使用

```c
static atomic_t test_atomic = ATOMIC_INIT(1);

/*字符设备操作函数集，open函数*/
static int led_chr_dev_open(struct inode *inode, struct file *filp)
{
    if (atomic_xchg(&test_atomic, 0) == 1) {
        printk("\n open from driver \n");
        return 0;
    } else {
        printk("\n driver on using! open failed !!!\n");
        return -EBUSY;
    }
}


static int led_chrdev_release(struct inode *inode, struct file *filp)
{
    atomic_set(&test_atomic,1);
	printk("KERN_ALERT  \n finished  !!!\n");

	return 0;
}
```

```c
#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>
#include <string.h>
int main(int argc, char *argv[])
{
    /*判断输入的命令是否合法*/
    if(argc != 2)
    {
        printf(" commend error ! \n");
        return -1;
    }

    /*打开文件*/
    int fd = open("/dev/rgb_led", O_RDWR);
    if(fd < 0)
    {
		printf("\n open file : /dev/rgb_led failed !!!\n");
		return -1;
	}


    /*写入命令*/
    int error = write(fd,argv[1],sizeof(argv[1]));
    if(error < 0)
    {
        printf("write file error! \n");
        close(fd);
        /*判断是否关闭成功*/
    }


    sleep(10);  //休眠10秒

    /*关闭文件*/
    error = close(fd);
    if(error < 0)
    {
        printf("close file error! \n");
    }
    return 0;
}
```

## 自旋锁/读写锁/顺序锁

include\linux\spinlock.h进行实现, 但具体实现依赖于架构, arch/arm64/include/asm/spinlock.h

- ARM64 自旋锁本质是一个 32 位整数，0 表示未加锁，1 表示已加锁。
- 加锁用原子指令循环尝试设置为1，解锁直接原子写0。
- 在等待锁的时候不会调度, 而是进行死锁, 为了避免优先级翻转, 获取自旋锁时，内核会自动禁用当前 CPU 的抢占（preempt_disable ()）；释放锁时，自动启用抢占（preempt_enable ()）

> 在临界区里面不可以使用会导致睡眠以及任务切换的函数

![image-20251109230831252](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109230831252.png)![image-20251109231005523](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109231005523.png)![image-20251109231243851](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109231243851.png)

在实际使用的时候, 中断里面尝试获取自旋锁会出现问题, 所以实际的应用的时候可可以关闭中断, 还有针对于下半部的关闭函数

在实际应用的时候有一个情况是可以**多个进行读取**, 但是**只有一个可以改**, 延伸出来**读写锁**

![image-20251109231649061](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109231649061.png)![image-20251109231711601](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109231711601.png)

顺序锁在读写锁的基础上衍生而来的，使用读写锁的时候读操作和写操作不能同时进行。使用**顺序锁**的话可以允许在**写的时候进行读操作**，也就是实现同时读写，但是不允许同时进行并发的写操作。

但是如果在读的过程中发生了写操作，**最好重新进行读取**，保证数据完整性。顺序锁保护的资源不能是指针，因为如果在写操作的时候可能会导致指针无效

seqlock（顺序锁）允许“多读单写”，但不是“读写完全同时进行”。它的机制是：

- 多个读者可以并发、无锁地读取数据（只要没有写者在写）。
- 写者写数据时，读者会检测到有写操作（通过序列号变为奇数），此时读者会等待（自旋）直到写者完成，或者重试读取。
- 写者之间是互斥的，不能同时写。
- 读者和写者不能真正“同时”访问同一份数据：如果读者在写者写入期间读取，读者会检测到数据不一致，自动重试，直到读到一致快照。

所以，seqlock 的本质是“读者无锁、写者加锁”，但写期间读者会自旋等待或重试，保证数据一致性。它适合读多写少、对偶尔重试能容忍的场景。

![image-20251109232007083](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109232007083.png)![image-20251109232026756](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109232026756.png)

#### 自旋锁

主要用于多核处理器, 短时间轻量级的等待, 被自旋锁保护的**临界区**一定**不能**调用任何能够引起**睡眠和阻塞**的

API 函数

- 短时期的轻量级加锁
- 加锁失败时，原地打转、忙等待

- 避免上下文调度、系统开销较小

**加锁步骤**：

1. 查看锁的状态，如果锁是空闲的
2. 将锁设置为当前线程持有

在没有 **CAS 函数**前，多个线程同时执行这 2 个步骤是会出错的。

CAS 函数把这 2 个步骤**合并为一条硬件级指令**。这样第 1 步比较锁状态和第 2 步锁变量赋值，将变为不可分割的原子指令(硬件同步原语)

自旋锁使用CPU 提供的 CAS 函数（Compare And Swap），在用户态代码中完成加锁与解锁操作

自旋锁并不一直"忙等待"，会与 CPU紧密配合 ，它通过 CPU 提供的 PAUSE 指令，**减少循环等待时的耗电量**；对于单核CPU，忙等待并没有意义，此时它会主动把线程休眠。

#### 自旋锁相关API

##### 定义自旋锁

```c
spinlock_t s_lock ;
```

##### 初始化自旋锁

```c
int spin_lock_init(spinlock_t *lock);
```

##### 获取自旋锁函数

```c
void spin_lock(spinlock_t *lock)
```

> 这个函数在尝试的时候会进行忙等待

##### 尝试获取自旋锁函数

````c
int spin_trylock(spinlock_t *lock)
````

> 尝试获取一次，获取成功返回“true”,获取失败返回“false”。程序继续往下执行

##### 释放自旋锁

```c
void spin_unlock(spinlock_t *lock)
```

## RCU

> 通常用于链表操作, 用于保护里面的数据 Documentation\RCU\whatisRCU.rst

RCU（Read-Copy-Update，读 - 复制 - 更新）是 Linux 内核中一种**读无锁、写延迟回收**的高性能同步机制，专为**读多写少**场景设计，核心是让读操作完全无锁、零开销，写操作通过**复制修改 + 原子替换 + 宽限期回收**三步实现并发安全

a. 移除对数据结构的指针，使后续读者无法再引用它。
b. 等待所有之前的读者完成RCU读侧临界区。
c. 此时没有读者再引用该数据结构，可以安全回收（如kfree）。

#### 读端临界区（Read-Side Critical Section）

- 用 `rcu_read_lock()` 标记开始，`rcu_read_unlock()` 标记结束。
- 作用：告知内核当前处于 RCU 读状态，**禁止抢占**（普通 RCU），保证读期间数据指针不被回收Linux Kernel。
- 约束：临界区内**不能睡眠、不能阻塞、不能进入用户态 /idle**。

**宽限期**

定义：写操作完成指针替换后，必须等待的**所有 CPU 都退出旧数据读临界区**的最短时间。

判断：所有 CPU 至少完成一次**上下文切换 / 用户态执行 /idle 循环**，即所有旧读者已退出Linux Kernel。宽限期结束后，旧数据无任何读者引用，可安全释放

#### 写端流程（Read-Copy-Update）

+ **复制**：写者不修改原数据，先复制一份副本并修改。
+ **原子替换**：用原子操作（如 `rcu_assign_pointer`）将全局指针指向新副本，后续读者读到新数据。
+ **等待宽限期**：调用 `synchronize_rcu()`（阻塞等待）或 `call_rcu()`（注册回调异步执行）。
+ **回收旧数据**：宽限期结束后，释放旧数据内存（如 `kfree_rcu`）。

> 常见的应用形式是链表

| 分类         | API 名称                                                     | 核心作用                             | 关键约束 / 注意事项                                          | 适用场景                        |
| ------------ | ------------------------------------------------------------ | ------------------------------------ | ------------------------------------------------------------ | ------------------------------- |
| **读端基础** | `rcu_read_lock()`                                            | 标记 RCU 读临界区开始                | 用于读者标记进入 RCU 读侧临界区。期间不能阻塞（但 CONFIG_PREEMPT_RCU 内核可抢占）。在 RCU 读侧临界区访问的 RCU 保护数据结构，在整个临界区内都不会被回收。可结合引用计数实现长期引用。 | 经典 RCU 读操作                 |
|              | `rcu_read_unlock()`                                          | 标记 RCU 读临界区结束                | 必须与`rcu_read_lock()`配对，不能嵌套异常（如 panic）        | 经典 RCU 读操作                 |
|              | `srcu_read_lock(struct srcu_struct *s)`                      | SRCU 读临界区开始                    | 允许睡眠 / 阻塞，返回值需传给解锁函数                        | 需睡眠的读场景（文件系统）      |
|              | `srcu_read_unlock(struct srcu_struct *s, int idx)`           | SRCU 读临界区结束                    | idx 为`srcu_read_lock`返回值，必须配对                       | 需睡眠的读场景（文件系统）      |
| **写端同步** | `synchronize_rcu()`                                          | 阻塞等待宽限期结束（所有旧读者退出） | 1. 不能在中断上下文调用2. 等待期间可睡眠, 注意 synchronize_rcu() 不会等待调用后新开始的读侧临界区 | 进程上下文写操作（简单场景）    |
|              |                                                              |                                      |                                                              |                                 |
|              | `synchronize_rcu_expedited()`                                | 快速等待宽限期（强制触发）           | 开销更高，仅紧急场景使用                                     | 需快速回收旧数据的写操作        |
|              | `synchronize_srcu(struct srcu_struct *s)`                    | 阻塞等待 SRCU 宽限期                 | 允许睡眠，可在进程上下文调用                                 | SRCU 写操作                     |
|              | `call_rcu(struct rcu_head *head, void (*func)(struct rcu_head *))` | 异步等待宽限期（注册回调）           | 1. 非阻塞，回调在软中断执行2. 不能在回调中睡眠3. `rcu_head`需关联旧数据,  synchronize_rcu() 的回调形式 | 中断 / 软中断上下文写操作       |
| **指针操作** | `rcu_dereference(p)`                                         | 安全读取 RCU 保护的指针              | 1. 必须在读临界区内使用2. 保证内存序，防止编译器 / CPU 重排, 用于读者安全地读取 RCU 保护的指针，带必要的内存屏障。通常将指针赋值给局部变量后再解引用。返回值只在当前 RCU 读侧临界区内有效。 | 读取 RCU 保护的全局指针         |
|              | `rcu_assign_pointer(p, v)`                                   | 原子替换 RCU 保护的指针              | 1. 写操作专用，保证指针可见性2. 无需读临界区, 标明哪些指针受 RCU 保护，以及结构体何时对其他 CPU 可见。通常通过 list_add_rcu() 等间接使用 | 更新 RCU 保护的全局指针         |
| **链表操作** | `list_add_rcu(struct list_head *new, struct list_head *head)` | 原子添加 RCU 链表节点                | 需在写锁保护下调用（防止写写竞争）                           | RCU 链表写操作                  |
|              | `list_del_rcu(struct list_head *entry)`                      | 原子删除 RCU 链表节点                | 删除后需等待宽限期再释放节点                                 | RCU 链表写操作                  |
|              | `list_replace_rcu(struct list_head *old, struct list_head *new)` | 原子替换 RCU 链表节点                | 替换后旧节点需等待宽限期释放                                 | RCU 链表更新操作                |
|              | `list_for_each_entry_rcu(pos, head, member)`                 | 遍历 RCU 保护的链表                  | 必须在读临界区内使用，只读遍历                               | RCU 链表读操作                  |
|              | `hlist_add_rcu(struct hlist_node *n, struct hlist_head *h)`  | 原子添加 RCU 哈希链表节点            | 需写锁保护                                                   | RCU 哈希表写操作                |
|              | `hlist_for_each_entry_rcu(pos, head, member)`                | 遍历 RCU 哈希链表                    | 读临界区内使用                                               | RCU 哈希表读操作                |
| **内存回收** | `kfree_rcu(ptr, rcu_head_member)`                            | 宽限期后释放内存                     | 1. `rcu_head_member`为结构体中`rcu_head`成员名2. 异步回收，非阻塞 | 替代`kfree`，RCU 保护的内存释放 |

### 示例

+ 链表

```c
// 定义RCU保护的链表
struct foo {
    int data;
    struct list_head list;
};
LIST_HEAD(g_foo_list);

// 读操作（无锁）
void foo_read(void) {
    struct foo *fp;
    rcu_read_lock(); // 进入读临界区
    list_for_each_entry_rcu(fp, &g_foo_list, list) {
        printk("%d\n", fp->data); // 安全访问
    }
    rcu_read_unlock(); // 退出读临界区
}

// 写操作（复制+替换+回收）
void foo_update(int old_data, int new_data) {
    struct foo *new_fp, *old_fp;
    // 1. 复制并修改
    new_fp = kmalloc(sizeof(*new_fp), GFP_KERNEL);
    new_fp->data = new_data;
    // 2. 原子替换
    rcu_read_lock();
    list_for_each_entry_rcu(old_fp, &g_foo_list, list) {
        if (old_fp->data == old_data) {
            // 标记需要替换
            list_replace_rcu(&old_fp->list, &new_fp->list);
            break;
        }
    }
    rcu_read_unlock();
    // 3. 等待宽限期并回收旧数据
    synchronize_rcu();
    kfree(old_fp);
}
```

+ 基础数据

```c
// 全局指针，用RCU保护
struct config *g_config __rcu;

// 读操作（无锁）
void read_config(void) {
    struct config *cfg;
    rcu_read_lock();
    cfg = rcu_dereference(g_config); // 安全读取指针
    if (cfg) {
        printk("config: %d\n", cfg->timeout);
    }
    rcu_read_unlock();
}

// 写操作（复制-替换-回收）
void update_config(int new_timeout) {
    struct config *new_cfg = kmalloc(sizeof(*new_cfg), GFP_KERNEL);
    struct config *old_cfg;
    new_cfg->timeout = new_timeout;
    
    // 原子替换指针
    old_cfg = rcu_replace_pointer(g_config, new_cfg, GFP_KERNEL);
    // 等待宽限期后回收旧配置
    synchronize_rcu();
    kfree(old_cfg);
}
```

`__rcu` 定义在内核头文件 `<linux/rcupdate.h>` 中，本质是给指针添加的`__attribute__`属性（不同架构 / 编译器会适配），核心定义简化后如下：

```c
// 核心定义（简化版）
#define __rcu __attribute__((noderef, address_space(4)))
```

- `noderef`：提示静态检查工具`sparse`—— 不能直接解引用这个指针（必须通过 RCU 接口）；
- `address_space(4)`：标记指针属于 RCU 地址空间，仅用于检查。

## 信号量

![image-20251109232105762](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109232105762.png)

计数器，用来控制对公共资源的访问, 不可以使用在中断里面, 因为会进入休眠

- 长时期的资源占用
- 获取信号量失败，触发上下文调度
- 当前进程(线程)陷入休眠，系统开销大

#### 信号量相关API

##### 定义信号量

```c
struct semaphore sem;
```

##### 初始化信号量

```c
static inline void sema_init(struct semaphore *sem, int val)
```

参数：

- sem：指定要初始化的信号量
- val：信号量的初始值

##### 获取信号量

```c
void down(struct semaphore *sem);
```

参数：

- sem：指定要初始化的信号量

##### 尝试获取信号量

```c
int down_trylock(struct semaphore *sem)
```

尝试获取一次信号量，获取成功返回0,获取失败返回非0值。

##### 释放信号量

```c
void up(struct semaphore *sem)
```

参数：

- sem：指定要初始化的信号量

## 互斥锁

![image-20251109232135269](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/mac-picture/image-20251109232135269.png)

#### 信号量与互斥锁：同步还是独占？

##### 信号量

- 强调信号机制(同步)
- 生产者--消费者模型
- 获取信号量失败，触发上下文调度

##### 互斥锁

- 强调互斥机制
- 独占共享资源

- 获取互斥锁失败，触发上下文调度

```
服务于多个线程间的执行的逻辑顺序的选信号量, 可以有多个资源
服务于共享资源的选互斥锁, 只可以使用一个资源
```

#### 自旋锁与互斥锁：休眠还是“忙等待”？

##### 自旋锁

- 加锁成本低，不释放cpu使用权

##### 互斥锁

- 加锁成本更高(上下文切换耗时在几十纳秒到几微秒之间)，加锁失败时会释放 CPU 给其他线程

```
无法判断锁住的代码会执行多久时，首选互斥锁
确定被锁住的代码执行时间很短，使用自旋锁取代互斥锁
```

#### 互斥锁相关API

##### 定义互斥锁

```c
struct mutex lock;
```

##### 初始化互斥锁

```c
void mutex_init(mutex *lock);
```

参数：

- lock：指定要初始化的互斥锁

##### 互斥锁加锁

```c
void mutex_lock(struct mutex *lock);
```

参数：

- lock：指定要加锁的互斥锁

##### 尝试获取互斥锁

```c
int mutex_trylock(struct mutex *lock);
```

尝试获取一次互斥锁，获取成功返回“true”,获取失败返回“false”。程序继续往下执行

##### 互斥锁解锁

```c
void mutex_unlock(struct mutex *lock);
```

参数：

- lock：指定要解锁的互斥锁

## 完成量

Documentation\scheduler\completion.rst

completion是一种代码同步机制，比锁/信号量更可取。如果有一个或多个线程必须等待某个内核活动达到某个点或某个特定状态，那么completion完成量可以为这个问题提供一个无竞争的解决方案。

总的来说，使用完成量需要三个步骤：

（1）初始化`struct completion`同步对象。

（2）通过调用`wait_for_completion()`函数等待完成量。

（3）调用`complete()`或`complete_all`发送完成信号。

```c
#define init_completion(x) __init_completion(x)
void wait_for_completion(struct completion *done);
void complete(struct completion *x);
void complete_all(struct completion *x);
```





