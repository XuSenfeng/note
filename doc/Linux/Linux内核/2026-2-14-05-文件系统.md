# 文件系统

是磁盘管理的目录, 也可以操作所有硬件和系统的功能机制

**initramfs 场景（现代主流）**：临时根文件系统上执行的第一个程序为 **/init**，负责加载驱动、准备并挂载真实根文件系统

**传统 initrd 场景**：临时根文件系统上执行的第一个程序为 **/linuxrc**，完成准备工作后退出，内核再执行真实根文件系统的 **/sbin/init**

**无临时根文件系统场景**：内核直接挂载真实根文件系统，依次查找执行 **/sbin/init → /etc/init → /bin/init → /bin/sh**

在根文件系统里面有各种的库, 程序, 以及配置文件, 各种设备节点

## linuxrc文件

可以在busybox的代码里面找到这个程序, init\init.c

1) 内核启动后执行 `/sbin/init`，而该路径是 BusyBox（或指向 BusyBox 的链接）；
2) initrd 环境下内核执行 `/linuxrc`（同样指向 BusyBox）；
3) 运行 `busybox init …` 或通过符号链接/硬链接以 init名称调用 BusyBox。

+ 它负责设置终端、环境变量、信号处理，并根据 inittab 或默认规则启动和监管其他进程
+ 定义了可用的 init 动作类型（`sysinit`/`respawn`/`askfirst`/`wait`/`once` 等）及其名称映射，后续解析 inittab 时使用
+ 维护一个链表 `init_action_list`，保存要执行的动作（命令、TTY、类型、PID），并提供添加/删除函数
+ **读取 `/etc/inittab`文件（或使用内置默认动作）并填充动作列表**，默认会添加 sysinit/askfirst shell、关机/重启处理等, 这个文件是一个配置文件, 默认会设置一些状态下面的运行命令
+ 安装信号处理（关机、重启、CAD、SIGHUP 重新加载 inittab），设置环境/控制台，解析 inittab，依次运行 SYSINIT→WAIT→ONCE，然后在主循环中持续处理 RESPAWN/ASKFIRST 并回收子进程
+ 运行**/etc/init.d/rcS**文件里面的程序(初始化的最初), 之后是WAIT和ONCE的, 然后循环处理RESPAWN和ASKFIRST的程序

> 设置了一系列的初始化命令, 之后依次执行命令, 最后等待循环命令栏退出
>
> - 解析 inittab（或默认规则）构建一组 init 动作（命令+类型+TTY）。
> - 启动阶段依次执行 `SYSINIT`→`WAIT`→`ONCE`。
> - 进入循环后反复处理 `RESPAWN`/`ASKFIRST`，并 `wait` 子进程退出，退出的会被标记并重启。
>
> 所以它既“按顺序执行初始化命令”，也“常驻等待子进程退出并重启”。如果你说的“等待某两款退出”是指等待子进程退出，这里是等待所有子进程的退出事件。

### 内核的使用

在linux3.1.2里面`init\main.c`, 查找文件系统里面的init程序, `start_kernel==>rest_init==>kernel_init==>init_post`

```c
/*
 * We try each of these until one succeeds.
 *
 * The Bourne shell can be used instead of init if we are
 * trying to recover a really broken machine.
 */
if (execute_command) { // 执行自定义的命令
    run_init_process(execute_command);
    printk(KERN_WARNING "Failed to execute %s.  Attempting "
                "defaults...\n", execute_command);
}
// 执行默认的程序
run_init_process("/sbin/init");
run_init_process("/etc/init");
run_init_process("/bin/init");
run_init_process("/bin/sh");
// 没有可以执行的, 内核启动失败
panic("No init found.  Try passing init= option to kernel. "
      "See Linux Documentation/init.txt for guidance.");
```

这里使用的参数execute_command是在init_setup函数里面配置的, 是uboot传进来的, `init=xxxx`的一个参数

```bash
static int __init init_setup(char *str)
{
	unsigned int i;

	execute_command = str;
	/*
	 * In case LILO is going to boot us with default command line,
	 * it prepends "auto" before the whole cmdline which makes
	 * the shell think it should execute a script with such name.
	 * So we ignore all arguments entered _before_ init=... [MJ]
	 */
	for (i = 1; i < MAX_INIT_ARGS; i++)
		argv_init[i] = NULL;
	return 1;
}
__setup("init=", init_setup);
```

使用的宏定义__setup会把这个函数记录在.init.setup段里面, 记录为obs_kernel_param结构体, 里面记录字符串密令和func两种数据

```c
struct obs_kernel_param {
	const char *str;
	int (*setup_func)(char *);
	int early;
};
```

这部分的命令在uboot参数解析的时候, 比如函数

```c
static int __init obsolete_checksetup(char *line);
static int __init do_early_param(char *param, char *val);
```

这两个函数的不同在于处理的阶段不同, `do_early_param`函数是在早期的处理里面处理标记为 `early` 的参数

`` 在常规参数解析中由 `unknown_bootoption` 调用，用来兜底处理“旧式/废弃”或常规的 `__setup` 参数

```c
//start_kernel()  
parse_early_param();
parse_args("Booting kernel", static_command_line, __start___param,
       __stop___param - __start___param,
       &unknown_bootoption);
```

unknown_bootoption函数调用`obsolete_checksetup`, 处理类似于`foo=bar,bar2 baz=fuz wiz`的参数

## 启动需要的文件

+ /dev/console使用的终端
+ init_main函数生成的linuxrc程序
+ /etc/init.d/rcS文件
+ shell相关的可执行文件
+ C语言的库glibc

## Linux文件系统支持

Linux里面对于文件系统的支持分为几个部分

1. Linux里面高速缓冲区的管理, 加载程序的时候按照需求加载, buffer.c
2. 文件系统的底层通用函数, 对于硬盘的读写分配释放, 对于目录节点inode的管理, 内存和磁盘的映射
3. VFS虚拟文件系统, 硬件驱动映射在文件系统里面, pipe管道

### ext2/3/4的逻辑结构

+ 引导块: 引导设备, 可以为空
+ 超级块: 文件系统的描述符
+ 数据块位图: 标记数据区哪个块能用
+ 索引i节点位图: 标记哪个inode空闲
+ 索引i节点表: 所有文件的元数据集合
+ 逻辑块/数据区

#### 文件节点

```c
// 这是在内存中的 i 节点结构。前 7 项与 d_inode 完全一样
struct m_inode {
	unsigned short i_mode; // 文件的类型和属性, ls -l的时候最前面的字母
	unsigned short i_uid; // 宿主的用户id
	unsigned long i_size; // 文件大小
	unsigned long i_mtime; // 修改的时间
	unsigned char i_gid; // 组id
	unsigned char i_nlinks; // 链接数
    // 普通文件
    // i_zone[0]~[6]：地址本前 7 页直接写盒子编号，拿东西最
    // i_zone[7]：一级间接块指针
    // i_zone[8]: 二级间接块指针, 存的是「一级间接块」的块号；
    // 每个一级间接块又能存大量数据块指针，相当于 “地址本的地址本”，用于大文件
    
    // 设备块文件(mount位置) sys_mknod
    // i_zone[0]：设备号
    
    // 管道文件
    // i_zone[0]：管道头指针（读位置）
	// i_zone[1]：管道尾指针（写位置）
	unsigned short i_zone[9]; // 映射在逻辑块的映射数组
/* these are in memory also */
	struct task_struct * i_wait;    // 等待该 i 节点的进程
	unsigned long i_atime;          // 最后访问时间 access time
	unsigned long i_ctime;          // i 节点自身修改时间 change time
	unsigned short i_dev;          //存储该文件所在的设备，dev_t中包括主设备号和次设备号
	unsigned short i_num;          // i 节点号
	unsigned short i_count;        // i 节点被使用的次数，0 表示该 i 节点空闲
	unsigned char i_lock;			// 锁定标志
	unsigned char i_dirt;			// 已修改(脏)标志
	unsigned char i_pipe;			// 管道标志
	unsigned char i_mount;         // 安装标志（挂载标志）
	unsigned char i_seek;          // 搜寻标志（用于lseek）
	unsigned char i_update;			// 更新标志
};
```

#### 超级块

fs\super.c

这个数据的获取需要使用缓冲区, 从磁盘里面读取到缓冲区再进行拷贝

这个块是对于一个设备的映射, 记录这个文件的挂载位置

```c
// 内存中磁盘超级块结构
struct super_block {
    // 记录在硬盘的部分
	unsigned short s_ninodes;        // 节点数
	unsigned short s_nzones;         // 逻辑块数
	unsigned short s_imap_blocks;    // i 节点位图所占用的数据块数
	unsigned short s_zmap_blocks;    // 逻辑块位图所占用的数据块数
	unsigned short s_firstdatazone;  // 第一个数据逻辑块号
	unsigned short s_log_zone_size;  // log(数据块数/逻辑块)大小
	unsigned long s_max_size;        // 文件最大长度
	unsigned short s_magic;
	/* These are only in memory 内存里面的部分 */
    // i 节点位图缓冲块在缓冲区里面的位置(占用 8 块，可表示 64M) 
    // i 节点位图最多占用 8 个缓冲块
	struct buffer_head * s_imap[8];    
	struct buffer_head * s_zmap[8];    // 逻辑块位图缓冲块指针数组
	unsigned short s_dev;              // 超级块所在的设备号
	struct m_inode * s_isup;           // 被安装的文件系统根目录的 i 节点。(isup-super i)
    // 执行 mount /dev/hda1 /mnt 时
    // s_imount 指向宿主文件系统中 /mnt 目录的 i 节点（挂载点）
	// s_isup 指向被挂载文件系统（/dev/hda1）的根目录 i 节点
	struct m_inode * s_imount;         // 被安装到的 i 节点
	unsigned long s_time;              // 修改时间
	struct task_struct * s_wait;       // 等待该超级块的进程
	unsigned char s_lock;              // 被锁定标志
	unsigned char s_rd_only;           // 只读标志
	unsigned char s_dirt;              // 已修改标志（脏标志）
};
```

#### 位图控制

fs\bitmap.c

这个文件里面使用函数free_block释放一个逻辑块, 同时把数据位图对应位置s_zmap置位1, 清空这个位的缓冲区, 这个函数会设置`b_dirt=0`可能导致未同步的信息丢失

使用函数`new_block`用于申请一个数据块, 通过位图查找第一个可以使用的数据块, 设置位图同时清空这部分的磁盘

在这个文件里面通过读取设置位图的方式, 实际的结构是对inode或者数据块进行申请以及释放

### 高速缓冲区

buffer.c

1. 映射关系
2. 应用和缓冲区之间的交互API
3. 缓冲区和磁盘之间的交互
4. 高速缓冲区的管理系统(循环链表 + 哈希表 + 单链表)

#### 代码逻辑

核心函数是`getblk`在缓冲区获得空闲缓冲块, 缓冲区有描述头, 里面和之后的实际缓冲区的位一一对应, 是一个结构体, 还记录有各个分区的其他信息

```c
 // 在程序中常用 bh 来表示 buffer_head 类型的缩写
struct buffer_head {
	char * b_data;			/* pointer to data block (1024 bytes) 数据块 */
	unsigned long b_blocknr;	/* block number 块号 */
	unsigned short b_dev;		/* device (0 = free) 数据源的设备号 */
	unsigned char b_uptodate;   // 更新标志，表示数据是否已经更新
	unsigned char b_dirt;		/* 0-clean,1-dirty */
	unsigned char b_count;		/* users using this block */  //使用的用户数, reference count? 
	unsigned char b_lock;		/* 0 - ok, 1 -locked 文件的锁 */
	struct task_struct * b_wait;        // 指向等待该缓冲区解锁的任务
    // 哈希表里面的链表
	struct buffer_head * b_prev;		// 前一块（这四个指针用于缓冲区的管理）
	struct buffer_head * b_next;		// 下一块
    // 空的缓冲区的链表
	struct buffer_head * b_prev_free;	// 前一空闲块
	struct buffer_head * b_next_free;	// 下一空闲块
};
```

这些块设备使用一个哈希表进行管理

```c
struct buffer_head * hash_table[NR_HASH];

// 初次调用   (0x300^0)%307 = 154
#define _hashfn(dev,block) (((unsigned)(dev^block))%NR_HASH)
#define hash(dev,block) hash_table[_hashfn(dev,block)]
```

```c
// buffer_memory  end~2M
extern int end;                   //外部传入，动态生成  整个内核代码的末尾地址
struct buffer_head * start_buffer = (struct buffer_head *) &end;
```

获取空闲的区域, 需要有一个指标判断一个位置的优先级

```c
// 下面宏定义用于同时判断缓冲区的修改标志和锁定标志，并且定义修改标志的权重要比锁定标志大
#define BADNESS(bh) (((bh)->b_dirt<<1)+(bh)->b_lock)
```

实际查询的时候, 首先在hush表里面查找这个块是不是已经被使用了, 没有使用的时候直接返回, 使用了的话继续寻找最小的

+ 表的创建

从结束的位置向前配置, 跳过被占用的位置, 配置出来一个链表

![image-20260217180927727](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202602171809842.png)

### inode节点

fs\inode.c

> 对应一个文件, 记录在磁盘的一个inode节点里面, 节点里面记录文件使用的数据块的索引

inode节点存储在硬盘里面, 但是在内存里面保留一个全局数组, 实际使用的是这个数组, 加载硬盘里面的数据暂存在数组里面

+ iget获得一个inode节点
+ iput释放一个inode节点
+ bmap: 对文件进行磁盘映射

> inode在硬盘里面记录的是d_inode, 内存里面是m_inode

#### 文件块操作

使用的底层函数是_bmap函数, 对一个文件进行块的获取和释放

```c
/**
 * @brief 块映射处理,为指定i节点的指定块号分配逻辑块
 * 
 * @param inode i节点
 * @param block 数据块号
 * @param create 创建标志，如果置位，则在对应逻辑块不存在时就申请新的磁盘块
 * @return int 数据块对应在设备上的逻辑块号
 */
static int _bmap(struct m_inode * inode,int block,int create);
```

在inode节点里面有一个i_zone里面记录的是这个节点的磁盘映射, 可以进行新的逻辑块的分配, i_zone里面记录的有7个表项, 一个一级表的位置, 一个二级表的位置, 最大是7+512+512*512个块

这个函数就是解析的函数, 返回的是**inode节点的某个数据块对应在设备上的逻辑块号**

查找到这个块不存在的时候, 使用函数`new_block`进行创建

#### iget/iput获取释放

iget和iput实际都是在操作内存里面的inode节点, 这个节点在同步的时候写入硬盘里面

从硬盘加载使用使用的是read_inode函数, 写入硬盘使用的是write_inode(操作的是硬盘的缓冲区)

在内存的inode表里面**预先分配一个空闲 i 节点缓冲位**，防止后续死锁, 如果这个表项里面的数据不是空的, 并且更改过了, 对这个表项进行写回(`get_empty_inode`), 之后检查一下这个inode对应的表项是不是已经被加载了, 是的话处理一下两种

+ 若该 i 节点标记为挂载点, 用被挂载设备号 `s_dev` 和根节点号 `ROOT_INO` 替换原来的 dev/nr，重新从表头扫描, 释放掉原挂载点 i 节点的引用，防止泄漏
+ 如果在缓冲表中找到 i 节点且不是挂载点，释放预分配的空闲位，直接返回该 i 节点

若整个缓冲表都未匹配到，则用预分配的空闲位加载硬盘数据

### 超级块

磁盘里面的超级块只是实际的使用数据的一部分, 加载在内存里面以后还会记录对应的挂载位置

```c
// 内存中磁盘超级块结构
struct super_block {
    // 磁盘里面的部分
	unsigned short s_ninodes;        // 节点数
	unsigned short s_nzones;         // 逻辑块数
	unsigned short s_imap_blocks;    // i 节点位图所占用的数据块数
	unsigned short s_zmap_blocks;    // 逻辑块位图所占用的数据块数
	unsigned short s_firstdatazone;  // 第一个数据逻辑块号
	unsigned short s_log_zone_size;  // log(数据块数/逻辑块)
	unsigned long s_max_size;        // 文件最大长度
	unsigned short s_magic;
	/* These are only in memory 内存里面的部分 */
	struct buffer_head * s_imap[8];    // i 节点位图缓冲块指针数组(占用 8 块，可表示 64M)
	struct buffer_head * s_zmap[8];    // 逻辑块位图缓冲块指针数组
	unsigned short s_dev;              // 超级块所在的设备号
	struct m_inode * s_isup;           // 被安装的文件系统根目录的 i 节点。(isup-super i)
	struct m_inode * s_imount;         // 被安装到的 i 节点
	unsigned long s_time;              // 修改时间
	struct task_struct * s_wait;       // 等待该超级块的进程
	unsigned char s_lock;              // 被锁定标志
	unsigned char s_rd_only;           // 只读标志
	unsigned char s_dirt;              // 已修改标志（脏标志）
};
```

所有的设备挂载在`struct super_block super_block[NR_SUPER];`这个结构体里面, 最多八个, 使用变量`int ROOT_DEV = 0;`记录根文件系统的设备号

+ `get_super`和`put_super`: 获取某个设备的超级块结构体, 查询数组里面的dev, 设置正在使用, 释放的时候不可以释放根设备, 或者被安装在某个i节点的设备, 释放一下两个位图的映射
+ `sys_umount`和`sys_mount`挂载卸载, 卸载的时候依据名字获取inode节点(比如/dev/hda1文件), 检查inode节点数组有没有这个设备里面的节点, 同步设备的缓冲区, 挂载的时候挂载的文件必须是一个目录, 之后就是设置标志位
+ `read_super`: 从磁盘里面加载, 读取对应的数据, 加载位图到缓冲区里面

#### Mount原理

- `super_block[i].s_imount`：指向挂载点所在原文件系统的 i 节点（目录节点）
- `super_block[i].s_isup`：指向被挂载文件系统的根 i 节点
- `m_inode.i_mount`：标志该 i 节点是挂载点

> **sys_mknod 的作用**（创建设备文件）：
>
> - 创建一个**特殊文件** i 节点（块设备或字符设备）
> - 设置 `i_mode = S_IFBLK`（块设备标志）
> - **将设备号存储在 `i_zone[0]` 中**
>
> **sys_mount 的依赖**（使用设备文件）：
>
> - 接收设备**文件路径** `dev_name`，通过 `namei()` 查找其 i 节点 [super.c:240](vscode-file://vscode-app/d:/vscode/Microsoft VS Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)
> - 从该 i 节点的 `i_zone[0]` **提取设备号** [super.c:243](vscode-file://vscode-app/d:/vscode/Microsoft VS Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)
> - 验证这是块设备文件 `S_ISBLK(dev_i->i_mode)` [super.c:244-246](vscode-file://vscode-app/d:/vscode/Microsoft VS Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)
> - 用提取的设备号读取文件系统超级块并挂载 [super.c:258-261](vscode-file://vscode-app/d:/vscode/Microsoft VS Code/resources/app/out/vs/code/electron-browser/workbench/workbench.html)
>
> **执行流程示例**：
>
> ```bash
> mknod /dev/hda1 b 3 1    ← sys_mknod: 创建块设备文件，i_zone[0] = 0x0301 (主设备3，次设备1)
> mount /dev/hda1 /mnt     ← sys_mount: 读 /dev/hda1 的 i 节点 → 取 i_zone[0] = 0x0301 → 用此设备号挂载
> ```
>
> **关键点**：
>
> - **mknod 先行**：必须先用 mknod 创建设备文件及其 i 节点
> - **mount 依赖 i_zone[0]**：mount 通过 i_zone[0] 这个**复用字段**获得设备号，而不是直接接收设备号参数

**Mount 执行流程**

1. **获取设备号**：通过设备文件名 `dev_name` 找到对应 i 节点，从中提取设备号
2. **获取挂载点**：通过目录路径 `dir_name` 找到目录 i 节点（挂载点），检查其引用计数为 1 且不是根目录
3. **读取被挂载文件系统的超级块**：通过设备号从磁盘读取超级块
4. 建立关联
    - `sb->s_imount = dir_i`：超级块记录挂载点 i 节点
    - `dir_i->i_mount = 1`：i 节点标记为挂载点

