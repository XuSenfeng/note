# 进程管理

![image-20260407211901022](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202604072119166.png)

## 运转

系统的滴答, jiffies(10ms/滴答), CPU内部的定时器, 上电的时候使用RTC时钟+kernel_mktime函数计算一下当前的时间, 存储在 startup_time 这个变量里, 同时使用startup_time记录启动时间

时钟的自加在函数timer_interrupt里面进行, 处理函数do_timer在(kernel\sched.c)

在do_timer里面依据当前的优先级更新时间, 处理用户时钟, 是一个定时器链表, 查看当前的任务是不是还有时间片, 处理调度, 在内核态的时候不进行调度

> 默认的时间片分配是当前的所有任务的时间片全部为0的时候, 重新分配时间片
>
> ```c
> (*p)->counter = ((*p)->counter >> 1) + (*p)->priority;
> ```
>
> 优先级时间片轮转

## 任务

每个进程有LDT局部描述符, 记录使用的段, 允许一个进程（或任务）拥有自己的段描述符, 以及TSS进程状态描述符, 描述CPU运行需要知道的数据, 主要是寄存器

> 两个表都是保存在gdt表里面

![image-20260407211949265](https://picture-01-1316374204.cos.ap-beijing.myqcloud.com/lenovo-picture/202604072119355.png)

```c
// 任务（进程）数据结构
struct task_struct {
/* these are hardcoded - don't touch */
    // 任务的运行状态（-1 不可运行，0 可运行(就绪)，>0 已停止）
	long state;	/* -1 unrunnable, 0 runnable, >0 stopped */
    //counter 值的计算方式为 counter = counter /2 + priority
    // 优先执行counter最大的任务;  任务运行时间计数(递减)（滴答数）(时间片)
	long counter;
    // 运行优先数。任务开始运行时 counter = priority，越大运行越长
	long priority;
    // 信号。是位图，每个比特位代表一种信号，信号值=位偏移值+1
	long signal;
    // 信号执行属性结构，对应信号将要执行的操作和标志信息
	struct sigaction sigaction[32];
    // 进程信号屏蔽码（对应信号位图）
	long blocked;	/* bitmap of masked signals */
	/* various fields */
	int exit_code;//任务执行停止的退出码，其父进程会取
    // 代码段地址,代码长度，数据长度，总长度，栈段地址
	unsigned long start_code,end_code,end_data,brk,start_stack;
    // 进程标识号，父进程号，父进程组号，会话号，会话首领
	long pid,father,pgrp,session,leader;
    // 用户标识号， 有效用户id，保存的用户id
	unsigned short uid,euid,suid;
    // 组标识号，有效组号，保存的组号
	unsigned short gid,egid,sgid;
    // alarm()设置, 报警定时器值, 到达的时候发送信号
	long alarm;	
    // 用户态运行时间，系统态运行时间，子进程用户态运行时间
    // 子进程系统态运行时间，进程开始运行时刻
	long utime,stime,cutime,cstime,start_time;
    // 是否使用了协处理器
	unsigned short used_math;
/* file system info */
    // 进程使用 tty 的子设备号。-1 表示没有使用
	int tty;		/* -1 if no tty, so it must be signed */	
	// 文件创建属性屏蔽位
    unsigned short umask;
    // 当前工作目录i节点
	struct m_inode * pwd;
    // 跟目录i节点
	struct m_inode * root;
    // 执行文件i节点, 判断系统中是否还有另一个进程在运行同一个执行文件
	struct m_inode * executable;
    // 执行时关闭文件句柄位图标志
    // 每个比特位代表一个文件描述符，用于确定在调用系统调用
    // execve()时需要关闭的文件描述符
	unsigned long close_on_exec;
    // 进程使用的文件表结构，用于保存文件句柄
	struct file * filp[NR_OPEN];
	/* ldt for this task 0 - zero 1 - cs 2 - ds&ss */
    // 为每个任务分配一个段描述表
    // 局部描述符段， 0-空，1-代码段 cs，2-数据和堆栈段 ds&ss
	struct desc_struct ldt[3];
	/* tss for this task */
    // 本进程的任务状态段信息结构
	struct tss_struct tss;
};
```

### 状态

```c
// 创建、就绪、执行、阻塞、终止 是进程的一般5种理论状态
// linux有7中状态
// 1.运行状态， 包括就绪态和运行态
// 2.可中断睡眠状态， 也就是阻塞状态，收到信号后可以执行信号处理函数
// 3.不可中断的睡眠状态， 磁盘IO时会出现这种状态，进程无法被中断，不能响应信号
// 4.停止状态
// 5.退出状态
// 6.僵尸状态， 子进程先于父进程退出，并且父进程没有调用wait或waitpid回收子进程。此时子进程即处于僵尸状态
// 7.跟踪状态， 当利用gdb调试某个程序，程序停留在某个断点处时，就处于跟踪状态
// ps -aux 查看进程状态
#define TASK_RUNNING		    0	// 进程正在运行或已准备就绪，就绪态+运行态
#define TASK_INTERRUPTIBLE	    1	// 进程处于可中断等待状态, 被信号唤醒, 比如使用waitpid, pause
#define TASK_UNINTERRUPTIBLE	2	// 进程处于不可中断等待状态，主要用于 I/O 操作等待, 只可以使用wakeup函数唤醒
#define TASK_ZOMBIE		        3	// 进程处于僵死状态，已经停止运行，但父进程还没发信号
#define TASK_STOPPED		    4	// 进程已停止00
```



### 任务切换

```c
/*
 *	switch_to(n) should switch tasks to task nr n, first
 * checking that n isn't the current task, in which case it does nothing.
 * This also clears the TS-flag if the task we switched to has used
 * tha math co-processor latest.
 * 切换任务
 */
#define switch_to(n) {\
struct {long a,b;} __tmp; \
__asm__("cmpl %%ecx,_current\n\t" \
	"je 1f\n\t" \
	"movw %%dx,%1\n\t" \
	"xchgl %%ecx,_current\n\t" \
	"ljmp %0\n\t" \
	"cmpl %%ecx,_last_task_used_math\n\t" \
	"jne 1f\n\t" \
	"clts\n" \
	"1:" \
	::"m" (*&__tmp.a),"m" (*&__tmp.b), \
	"d" (_TSS(n)),"c" ((long) task[n])); \
}
```

**TSS（Task State Segment）**：x86 保护模式下的特殊内存段，专门用于保存进程的完整上下文（寄存器、栈地址、特权级等），每个进程对应一个 TSS；

**ljmp 指令**：x86 的长跳转指令，若跳转到 TSS 段选择子，会触发**硬件自动完成上下文切换**（保存当前进程上下文到旧 TSS，加载目标进程上下文从新 TSS）；长跳转到 `__tmp.a`（`%0`）指向的 TSS 段

> 这里的a是一个占位符, 实际使用的是b里面记录的数字

**CR0 寄存器的 TS 位**：Task Switched 位，进程切换时 CPU 会自动置 1，此时执行浮点指令会触发异常；`clts` 指令用于清除该位，允许新进程使用浮点协处理器（FPU）。

`dx` 中是目标进程的 TSS 段选择子（`_TSS(n)`），写入 `__tmp.b`（`%1`）

## 调度器

### 调度器初始化

在Linux的main函数里面有`sched_init()`在文件(kernel\sched.c)里面初始化调度器

在这个函数里面初始化init任务的tss和, ldt表, 记录在gdt表里面, 同时清空之后的表项

### 任务创建fork

对应的文件是kernel\fork.c是一个系统调用

1. 在task链表里面找一个空位存储进程
2. 创建一个task_struct
3. 设置当前的task_struct

```assembly
.align 2
_sys_fork:
	call _find_empty_process # 查找空位
	testl %eax,%eax
	js 1f
	push %gs
	pushl %esi
	pushl %edi
	pushl %ebp
	pushl %eax
	call _copy_process
	addl $20,%esp
1:	ret
```

_copy_process实际就是对当前进程里面数据的复制, 创建一个新的代码数据段, 对立面的数据进行拷贝, 子进程继承父进程使用的文件

### 睡眠sleep_on

设置任务的状态是不可中断的状态, 同时使用一个任务指针指向当前的任务, 在当前的任务的栈里面存储之前的睡眠任务(tmp变量), 这样在被唤醒的时候设置之前存储的睡眠任务状态为RUNNING

```c
// 将当前任务置为不可中断的等待状态,并让睡眠对象的任务指针指向当前任务
// 只有明确地唤醒时才会返回
void sleep_on(struct task_struct **p)
{
	struct task_struct *tmp;

	if (!p)
		return;
	if (current == &(init_task.task))
		panic("task[0] trying to sleep");
	tmp = *p; // 指向之前的睡眠进程
	*p = current;
	current->state = TASK_UNINTERRUPTIBLE;
	schedule();
	if (tmp)
		tmp->state=0; // 唤醒上一个任务TASK_RUNNING = 0
}
```

### 释放exit

子进程(do_exit)

+ 释放使用的内存
+ 关闭打开的文件
+ 让1,init进程管理创建的子进程
+ 如果当前进程是一个会话头的进程, 删除所有的进程(kill_session)
+ 改变进程的状态, 变为僵死进程, 向父进程发送信号(tell_father)

父进程(wait, waitpid)

+ 等待子进程的终止
+ 收到信号的时候终止子进程
+ 累加子进程的运行时间到自己
+ 释放子进程的进程描述结构体(release)





### 任务

#### idle任务

默认的任务在完成初始化以后fork出来init任务, 之后在pause里面进行循环

```c
if (!fork()) {		/* we count on this going ok */       // 进程1
    init();                                               //child process
}
/*
 *   NOTE!!   For any other task 'pause()' would mean we have to get a
 * signal to awaken, but task0 is the sole exception (see 'schedule()')
 * as task 0 gets activated at every idle moment (when no other tasks
 * can run). For task0 'pause()' just means we go check if some other
 * task can run, and if not we return here.
 */
for(;;) pause();
```



#### init任务

打开标准的输入输出文件, 创建一个子进程(PID2), 开启输入是/etc/rc文件, 之后执行/bin/sh, 这样启动的 shell 会从 `/etc/rc` 读取启动脚本命令，完成系统初始化配置；脚本执行完这个子进程退出

在父进程等待子进程的退出, 推出之后再次创建一个进程, 在子进程里面处理SHELL, 重新打开标准输入输出, 父进程等待, 如果出错退出打印日志, 重新进入SHELL

> 两次创建的时候使用的环境变量是不同的, 目前只有一个`HOME`变量
>
> ```c
> static char * argv_rc[] = { "/bin/sh", NULL };
> static char * envp_rc[] = { "HOME=/", NULL };
> 
> static char * argv[] = { "-/bin/sh",NULL };
> static char * envp[] = { "HOME=/usr/root", NULL };
> ```
>
> 

```c
void init(void)
{
	int pid,i;

	setup((void *) &drive_info);
	(void) open("/dev/tty0",O_RDWR,0);                        // 返回的句柄号 0 -- stdin 标准输入设备
	(void) dup(0);                                            // 复制句柄，产生句柄 1 号 -- stdout 标准输出设备
	(void) dup(0);                                            // 复制句柄，产生句柄 2 号 -- stderr 标准出错输出设备
	printf("%d buffers = %d bytes buffer space\n\r",NR_BUFFERS,
		NR_BUFFERS*BLOCK_SIZE);                               // 打印缓冲区块数和总字节数，每块 1024 字节
	printf("Free mem: %d bytes\n\r",memory_end-main_memory_start);
	if (!(pid=fork())) {                                      // 进程1创建进程2！！！！
		close(0);
		if (open("/etc/rc",O_RDONLY,0))         // 以只读形式打开rc文件（配置文件），占据0号文件描述符
			_exit(1);
		execve("/bin/sh",argv_rc,envp_rc);       // 加载shell程序. 此shell程序读取rc文件并启动update进程，用于同步缓冲区的数据到外设
		_exit(2);                                // 每隔一段时间，update程序会被唤醒
	}
	// shell加载普通文件 如/etc/rc 后会退出，所以下面需要重新启动一个shell
	// shell加载字符设备文件不会退出， 如/dev/tty0
	if (pid>0)                                                //parent process
		while (pid != wait(&i))                               //父进程等待子进程的结束。&i 是存放返回状态信息的位置。
			/* nothing */;
		while (1) {                                               //创建的子进程的执行已停止或终止,再创建一个子进程
			if ((pid=fork())<0) {
				printf("Fork failed in init\r\n");
				continue;
			}
			if (!pid) {
				close(0);close(1);close(2);
				setsid();
				(void) open("/dev/tty0",O_RDWR,0);     // 标准输入设备 stdin
				(void) dup(0);                         // 标准输出设备 stdout
				(void) dup(0);                         // 标准错误输出设备 stderr
				_exit(execve("/bin/sh",argv,envp));
			}
			while (1)
				if (pid == wait(&i))
					break;
			printf("\n\rchild %d died with code %04x\n\r",pid,i);
			sync();
		}
	_exit(0);	/* NOTE! _exit, not exit() */
}
```

